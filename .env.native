# 原生 iOS 壳构建（vite build --mode native）专用。
# 原生 app 没有"同源"，必须用绝对的 https 地址连后端；
# api.ts 的 asrWsUrl() 会据此自动得到 wss://.../app/api/asr。
# 换成你自己的公网 HTTPS 域名即可。
VITE_API_BASE=https://139-224-226-80.sslip.io/app/api
